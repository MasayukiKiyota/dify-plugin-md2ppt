# Privacy Policy

## Data Collection

このプラグインは、ツールの実行時に以下のデータを受け取ります。

- 変換対象の Markdown 本文（`markdown_text`）またはアップロードされた Markdown ファイル（`markdown_file`）
- アップロードされた PowerPoint テンプレート（`template_file`）
- マッピング設定の YAML（`config_yaml`）と出力ファイル名（`file_name`）

認証情報は一切必要とせず、収集もしません。

## Data Processing

すべての処理はプラグインのプロセス内で完結します。

- Markdown の解析と PowerPoint の生成はメモリ上で行われます。
- アップロードされたテンプレートは、ツール呼び出しごとに作成される一時ディレクトリに
  書き出され、`.potx` の場合はコンテンツタイプのみを正規化します。この一時ディレクトリと
  その内容は、ツール呼び出しの終了時に削除されます。
- 生成された `.pptx` はメモリ上で組み立てられ、Dify にファイルとして返されます。

## Third-party Services

このプラグインは外部サービスへの通信を一切行いません。ネットワークアクセスは発生しません。

Markdown 内に `http(s)://` の画像 URL が含まれていても、取得は行わず、
「画像が見つかりません」というプレースホルダに置き換えられます。

## Data Retention

データは保持されません。ツール呼び出しの終了とともに、一時ディレクトリは削除され、
メモリ上のデータは解放されます。ログにファイルの内容が記録されることはありません。

## Data Security

- 認証情報を扱わないため、認証情報の漏洩リスクはありません。
- 一時ファイルは OS の一時ディレクトリ配下に呼び出しごとに作成され、確実に削除されます。
- Markdown 内の画像参照は、この一時ディレクトリを基準に解決されます。絶対パスを書いた
  場合はプラグインコンテナ自身のファイルシステムを参照しますが、その内容が外部に
  送信されることはありません。

## User Rights

このプラグインは個人データを保存しないため、削除・訂正・エクスポートの請求先となる
データは存在しません。処理対象のデータの管理は、利用者の Dify インスタンスに従います。

## Contact Information

このプラグインに関するお問い合わせは、提供元（masayukikiyota）までご連絡ください。

## Changes to This Policy

本ポリシーが更新された場合は、プラグインのバージョン更新とあわせて本ファイルを改訂します。

Last updated: 2026-08-27
